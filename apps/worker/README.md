# Worker

Node worker package boundary for future Railway background jobs.

GOV-01 includes the package shell only. Mailbox sync, parser jobs, reconciliation polling, and retry behaviour are deferred to later PR-governed feature tasks with explicit safety tests.
