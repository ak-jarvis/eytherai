# Web

Next.js frontend for Eyther Phase 1.

- Local URL: `http://localhost:3000`
- Frontend hosting target: Vercel
- API base URL: `NEXT_PUBLIC_API_BASE_URL`, defaulting to `http://localhost:3001/api/v1`

The web app must not access Postgres directly. It reads through API contracts only.
